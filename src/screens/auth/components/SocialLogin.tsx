import { View, Text } from 'react-native'
import React from 'react'
import { SectionCP, TextCP } from '../../../components/customs'

const SocialLogin = () => {
  return (
    <SectionCP>
      <TextCP text=''/>
    </SectionCP>
  )
}

export default SocialLogin